[client]
remote_addr = "$RATHOLE_REMOTE_ADDR"
default_token = "$RATHOLE_TOKEN"

[client.transport]
type = "tcp"

[client.transport.tcp]
nodelay = true
keepalive_secs = 10
keepalive_interval = 5

[client.services.$RATHOLE_SERVICE_NAME]
local_addr = "$RATHOLE_LOCAL_ADDR"
